###命名规范
基本原则
1、动词开头：明确测试操作，如 验证、检查、测试、确认
2、包含核心对象：如接口名称、页面元素、功能模块
3、体现预期结果：如 成功、失败、符合预期
4、避免冗余：不用 “的”“进行” 等虚词，不重复模块名

```python
import allure
import pytest
@allure.epic('平台端接口测试')
@allure.feature('登录-登录模块')
@allure.story('登录模块接口测试')
class TestDemo:
    @allure.title("验证用户登录接口-正确账号密码返回200")  # 推荐
    def test_user_login_success(self):
        pass
    
    @allure.title("检查订单列表-未登录状态下跳转登录页")  # 推荐
    def test_order_list_unlogin(self):
        pass
```

###断言规范
1、定义了断言类AssertUtils
2、断言方法assert_status_code() # 状态码断言
3、断言方法assert_response_field() # 接口相应字段断言
4、断言方法assert_value_equal()  # 值相等断言
5、断言方法assert_value_in() # 值包含断言
6、参数说明：actual 为实际返回的响应数据 expected 为预期返回的响应数据 check_desc 为断言描述
```python
import  AssertUtils
from api.product_guarantee_list_api import ProductGuaranteeListApi
api = ProductGuaranteeListApi()
resp = api.send()
AssertUtils.assert_status_code(actual_code = resp.status_code, expected_code =200,case_desc = '删除商品分类接口，状态码断言200')
AssertUtils.assert_response_field(resp_json = resp.json(), required_fields = ['code', 'message', 'data'], case_desc = '删除商品分类接口，接口相应核心字段断言')
AssertUtils.assert_value_equal(actual = resp.json()['code'], expected = 6202, check_desc = '删除商品分类接口，code断言')
AssertUtils.assert_value_in(actual=resp.json()['message'], container = '有品牌关联该分类，无法删除' , check_desc = '删除商品分类接口，message断言')
```